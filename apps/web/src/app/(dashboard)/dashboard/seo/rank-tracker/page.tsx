import { RankTrackerClient } from "./rank-tracker-client";

export const metadata = { title: "Rank Tracker — EasyFetcher" };

export default function RankTrackerPage() {
  return <RankTrackerClient />;
}
